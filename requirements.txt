# YouTube Audio Transcriber Dependencies
# Python 3.9+ required

# CLI Framework (adjusted for Python 3.9 compatibility)
click>=8.1.8

# YouTube audio downloading
yt-dlp>=2024.3.10

# Audio processing (FFmpeg-based with PyDub fallback)
pydub>=0.25.0

# Optional: High-quality audio processing (not required after FFmpeg optimization)
# librosa>=0.10.0
# soundfile>=0.12.0

# AI Transcription with OpenAI gpt-4o-transcribe
openai>=1.54.0

# Google Gemini API for transcription and text processing
google-genai>=1.0.0

# Environment variables loading
python-dotenv>=1.0.0

# Interactive CLI interface for entity review
inquirer>=3.1.0

# Optional: FFmpeg for audio processing (requires system installation)
# Note: FFmpeg must be installed separately on your system
# - Windows: Download from https://ffmpeg.org/download.html
# - macOS: brew install ffmpeg
# - Linux: sudo apt install ffmpeg (Debian/Ubuntu) or equivalent

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.7.0
# flake8>=6.0.0
# mypy>=1.5.0 